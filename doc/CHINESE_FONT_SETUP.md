# 中文字体设置指南

## 🎯 问题

Railway 部署环境没有中文字体，导致 PDF 中的中文显示为乱码或方框。

## ✅ 解决方案

将中文字体文件打包到项目中，确保 PDF 能正确显示中文。

---

## 🚀 快速设置

### 方法一：自动下载（推荐）

在 `backend` 目录下运行：

```bash
npm run download-font
```

这会自动尝试从多个源下载中文字体。

### 方法二：手动下载

如果自动下载失败，请手动下载：

#### 选项 A：Noto Sans SC（推荐，较小）

1. 访问 [Google Fonts - Noto Sans SC](https://fonts.google.com/noto/specimen/Noto+Sans+SC)
2. 点击 **"Download family"** 按钮
3. 解压下载的 ZIP 文件
4. 找到 `NotoSansSC-Regular.ttf` 文件
5. 复制到 `backend/src/assets/fonts/NotoSansSC-Regular.ttf`

#### 选项 B：Source Han Sans（Adobe）

1. 访问 [Adobe Source Han Sans Releases](https://github.com/adobe-fonts/source-han-sans/releases)
2. 下载 `SourceHanSansCN-Regular.otf`
3. 保存到 `backend/src/assets/fonts/SourceHanSansCN-Regular.otf`

---

## 📁 文件结构

下载完成后，文件结构应该是：

```
backend/
├── src/
│   ├── assets/
│   │   └── fonts/
│   │       ├── NotoSansSC-Regular.ttf  ← 字体文件
│   │       └── README.md
│   └── report/
│       └── pdf.service.ts
└── scripts/
    └── download-chinese-font.js
```

---

## 🚢 Railway 部署

### 自动部署

字体文件会自动包含在 Git 仓库中，Railway 部署时会自动包含：

1. 确保字体文件已提交到 Git：
   ```bash
   git add backend/src/assets/fonts/*.ttf
   git add backend/src/assets/fonts/*.otf
   git commit -m "Add Chinese font files"
   git push origin main
   ```

2. Railway 会自动检测并部署

### 验证部署

部署完成后，查看 Railway 日志，应该看到：

```
✅ 成功加载中文字体: /app/src/assets/fonts/NotoSansSC-Regular.ttf
```

如果看到：

```
⚠️ 未找到中文字体文件，使用 Courier 字体
```

说明字体文件未正确部署，请检查：
1. 字体文件是否在 `src/assets/fonts/` 目录
2. 文件是否已提交到 Git
3. Railway 部署日志中是否有字体文件

---

## 🔍 故障排查

### 问题 1：字体文件太大

**解决方案**：
- 使用 Noto Sans SC（约 5MB）
- 或使用字体子集工具提取需要的字符

### 问题 2：下载失败

**解决方案**：
- 使用 VPN 或代理
- 手动下载（见方法二）
- 使用其他字体源

### 问题 3：字体文件未加载

**检查清单**：
- [ ] 字体文件在 `src/assets/fonts/` 目录
- [ ] 文件名正确（区分大小写）
- [ ] 文件已提交到 Git
- [ ] Railway 部署包含字体文件
- [ ] 查看 Railway 日志确认路径

---

## 📊 字体文件大小对比

| 字体 | 大小 | 来源 |
|------|------|------|
| Noto Sans SC | ~5MB | Google Fonts |
| Source Han Sans CN | ~10MB | Adobe |
| 系统字体 | 0MB | 系统自带 |

---

## 💡 最佳实践

1. **使用 Noto Sans SC**（较小，质量好）
2. **提交字体文件到 Git**（确保部署一致性）
3. **添加 .gitignore 排除**（如果不想提交，但需要手动部署）

### 如果不想提交字体文件到 Git

在 `.gitignore` 中添加：

```
backend/src/assets/fonts/*.ttf
backend/src/assets/fonts/*.otf
```

然后在 Railway 部署时手动上传字体文件，或使用构建脚本下载。

---

## 🎉 完成

设置完成后，PDF 中的中文应该能完美显示了！

---

最后更新：2026-01-07

